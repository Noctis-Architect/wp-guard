# Copy to .env (install-systemd.sh creates this automatically)
# Authorized use only — scan sites you own or have written permission to test.
WP_SCANNER_SECRET=change-me-to-a-long-random-string
WP_SCANNER_PORT=5000
